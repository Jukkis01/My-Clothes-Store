/*import React from "react";
import { dummydata } from "../assets/dummydata";
import "../App.css";

const MenSocks = () => {
  const socks = dummydata.filter(
    (item) => item.category === "Men" && item.type === "Socks"
  );

  return (
    <div className="product-list">
      {socks.map((sock) => (
        <div key={sock.id} className="product">
          <img src={sock.image} alt={sock.name} className="product-image" />
          <h2>{sock.name}</h2>
          <p>Color: {sock.color}</p>
          <p>Price: {sock.price}</p>
          <p>{sock.description}</p>
        </div>
      ))}
    </div>
  );
};

export default MenSocks;*/
